{
  "name": "podcast-note",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Podcast intelligence platform: watch topics, process podcasts, and generate grounded insights.",
  "scripts": {
    "m0:demo": "bun apps/worker/src/cli.ts demo",
    "m0:process-transcript": "bun apps/worker/src/cli.ts process-transcript",
    "check:imports": "bun apps/worker/src/check-imports.ts"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.3.0"
  }
}
